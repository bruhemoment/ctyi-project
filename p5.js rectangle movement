let x, y;

function setup() {
  createCanvas(400, 400);
 x = 200
  y = 100
    
}

function draw() {
  background(200);
  
  
  stroke(50);
  fill(100);
  rect(x,y,250,50);
  
  x = x - 1;

  if (x < 0) {
    x = width;
  }
}
